 #try:
       # if not  cves:
           # return 
        #cve_id = cves.get('cve')
        #if not cve_id:
               # return 
        #if cve_id  not in posted_cves:
               # posted_cves.add(cve_id)
                #channel = bot.get_channel(channel_id)
               # except Exception as e:
        #print(f"Cannot Fetch CVE's: {e}")
 